machine:
  services:
    - docker

dependencies:
  override:
    - docker info

test:
  pre:
    - ./helper.sh build run:neo1,neo2,neo3
  override:
    - for i in $(docker ps | grep 7474 | sed -r 's/.*:(.....)->7474.*/\1/'); do curl -sSL http://localhost:$i; done
