machine:
  services:
    - docker

dependencies:
  override:
    - docker info

test:
  pre:
    - ./helper.sh clear:all build run:neo1,neo2,neo3
  override:
    - curl localhost:$(docker ps | grep 7474 | sed -rn '1s/.*:(.....)->7474.*/\1/p')
    - curl localhost:$(docker ps | grep 7474 | sed -rn '2s/.*:(.....)->7474.*/\1/p')
    - curl localhost:$(docker ps | grep 7474 | sed -rn '3s/.*:(.....)->7474.*/\1/p')
